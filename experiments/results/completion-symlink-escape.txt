== conditions
date              2026-09-09T18:42:05Z
host kernel       6.18.44-fc-v24
podbox            podbox 0.1.0
image             public.ecr.aws/docker/library/alpine:3.20

rootfs            <WORK>/store/rootfs/sha256_c64c687cbea9300178b30c95835354e34c4e4febc4badfe27102879de0483b5e/rootfs
planted           5 symlinks out of the rootfs, and 1 legitimate one inside

== the run
  exit              0
  fixups reported   13
    podbox: complete: created dev/null (dev-node, T-0401): a real character device 1:3. This machine permits mknod(2), so podbox made the node rather 
    podbox: complete: created dev/zero (dev-node, T-0401): a real character device 1:5. This machine permits mknod(2), so podbox made the node rather 
    podbox: complete: created dev/random (dev-node, T-0401): a real character device 1:8. This machine permits mknod(2), so podbox made the node rathe
    podbox: complete: created dev/urandom (dev-node, T-0401): a real character device 1:9. This machine permits mknod(2), so podbox made the node rath
    podbox: complete: rewrote etc/resolv.conf (resolv.conf, T-0402): this machine's /etc/resolv.conf, 67 bytes. ⛔ It was a symlink to <WORK>/canary/
    podbox: complete: rewrote etc/hosts (hosts, T-0403): localhost
    podbox: complete: rewrote etc/passwd (identity, T-0404): the image has no etc/passwd (Symlink("<WORK>/canary/passwd")), so tooling that calls getp
    podbox: complete: rewrote etc/mtab (mtab, T-0405): it was a symlink to <WORK>/canary/mtab. ⛔ podbox REPLACED the link rather than writing throug
    podbox: complete: created etc/apt/apt.conf.d/99podbox (apt-sandbox, T-0407): APT::Sandbox::User root. The _apt user is not mapped on this runtime
    podbox: complete: failed etc/ssl/certs/ca-certificates.crt (ca-bundle, T-0407): etc/ssl/certs is a symlink to <WORK>/canary/dir, which does not re
    podbox: complete: created etc/ssl/ca-bundle.pem (ca-bundle, T-0407): the image had no bundle here; installed this machine's, from /root/.ccr/ca-bu
    podbox: complete: created etc/ssl/cert.pem (ca-bundle, T-0407): the image had no bundle here; installed this machine's, from /root/.ccr/ca-bundle.
    podbox: complete: 11 fixup(s) applied to this image's rootfs, 3 degraded, 1 failed, 0 step(s) to run inside it; libc=musl. ⛔ These are edits pod

== the canaries, read off the filesystem
  A /etc/mtab           : intact
  B /etc/resolv.conf    : intact
  C /etc/hosts          : intact
  E /etc/passwd         : intact
  D a directory link out: intact

== and the writes landed INSIDE
  etc/mtab is a regular file inside the rootfs and carries what podbox wrote
  etc/resolv.conf is a regular file inside the rootfs and carries what podbox wrote
  etc/hosts is a regular file inside the rootfs and carries what podbox wrote
  etc/passwd is a regular file inside the rootfs and names root

== F: a legitimate INTERNAL directory link is followed
  etc/apt -> /var/aptreal was followed, and the drop-in is at its target
  D: refused, and the refusal is on the banner:
       podbox: complete: failed etc/ssl/certs/ca-certificates.crt (ca-bundle, T-0407): etc/ssl/certs is a symlink to <WORK>/canary/dir, whic

== verdict
  6 doors, 0 escapes; the five that leave were confined and the one
  that stays inside was followed.
