== conditions
date              2026-09-10T00:28:13Z
host kernel       6.18.44-fc-v24
podbox            podbox 0.1.0
image             public.ecr.aws/docker/library/alpine:3.20
flag-error code   125
cli-error code    1

== 1. TOOL.md section 9's three
  run --network=none                 rc=125  ok
      podbox run: --network is in the parity table with status None: there is no network namespace to select
  run -v host:/mapped:ro             rc=125  ok
      podbox run: -v, --volume is in the parity table with status None: podbox cannot mount(2) on this runtime, so a volume 
  a Go payload under interpose        SKIPPED: M6 has no interposer yet
      TODO/interpose.md T-0709 is the entry, and this clause is its Prove

== 2. the honesty switch: --strict refuses a degraded run (T-0804)
  run --strict                       rc=125
      podbox run: --strict, and this run is degraded in 4 way(s). podbox refuses rather than running and letting the pay
  reasons named                      4
  the same run without --strict      rc=0 (must be 0)
  --strict against an image with a step  rc=125, step reasons 1
        - podbox would run `/usr/bin/openssl rehash /etc/ssl/certs` (T-0412) inside this image before the payload: /etc/

== 3. -t is a NAMED refusal where /dev/ptmx is unusable (T-0503)
  /dev/ptmx usable here              true
  run -t                             rc=0 (a pty is available, so 0)
  ⚠ the refusal arm could not be measured here: this machine has a pty

== 4. the table decides, and an unlisted flag cannot be quietly accepted
  an unlisted flag                   rc=125  ok
      podbox run: unknown option "--no-such-flag". It has no row in the parity table; `podbox system info` lists every flag 
  a None flag names its status       rc=125  ok
      podbox run: --privileged is in the parity table with status None: podbox holds every capability bit already and can st
  a None VERB names its reason       rc=125  ok
      podbox: stats: resource accounting needs a cgroup this runtime does not grant

== 5. podbox speaks HTTPS only, and never downgrades (T-0201)
  pull http://...                      rc=1 (124 would be the hang)
      HTTPS only and will not downgrade

== 6. a container podbox did not see end has NO exit code (T-0604)
  after SIGKILL of the launcher      state=dead exitcode=- wait rc=125

== 7. the attribution census is 0 or 2, never 1 (T-1109's own rule)
  30-attribution-census.sh           rc=2
      it could not run here, which is the third state

== verdict
  every refusal that could be driven here happened AND named its reason.
