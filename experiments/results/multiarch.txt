== conditions
date              2026-09-09T13:27:03Z
host kernel       6.18.44-fc-v24
host arch         x86_64
rustc             rustc 1.98.1 (48a229cea 2026-09-01)
zig               0.16.0
qemu-aarch64      qemu-aarch64 version 8.2.2 (Debian 1:8.2.2+ds-0ubuntu1.18)

== 1. every claimed architecture compiles the whole workspace
  ok    x86_64-unknown-linux-musl          workspace checks
  ok    aarch64-unknown-linux-musl         workspace checks
  ok    riscv64gc-unknown-linux-musl       workspace checks
  ok    loongarch64-unknown-linux-musl     workspace checks
  ok    armv7-unknown-linux-musleabihf     workspace checks
  ok    i686-unknown-linux-musl            workspace checks
  ok    powerpc64le-unknown-linux-musl     workspace checks
  ok    s390x-unknown-linux-gnu            workspace checks

== 2. the architectures that do NOT build, and exactly why
  none. ⭐ T-0912 cleared the one there was: podbox no longer takes
  its syscall trap from a crate that gates five architectures behind
  nightly. `crates/podbox-probe/Cargo.toml` target-gates `syscalls`
  away on those five and `sys.rs` reads linux-raw-sys instead.
  ⚠ mips, mips64 and 32-bit powerpc are NOT in clause 1 and are not
  silently skipped either: sys.rs answers them with a compile_error
  naming what is missing, because o32 passes arguments five and six
  on the stack and a convention nobody has run is a claim.

== 3. is the crate's nightly gate still true of this rustc?
  ⚠ powerpc64 inline asm COMPILES on rustc 1.98.1 (48a229cea 2026-09-01)
    so the crate gate is stale, not a rustc limit. ⭐ T-0912 took the
    second of the two options this clause named: podbox carries its
    own trap and reads the numbers from linux-raw-sys, and clause 1
    now includes powerpc64le. This clause stays because the day a
    syscalls release drops the gate is worth knowing too.

== 4. podbox RUNS on aarch64, and what that sentence is worth
  binary          ELF 64-bit LSB executable, ARM aarch64, version 1 (SYSV), 
  bytes           2128856
  binfmt for aarch64 registered: no   ⚠ the probe re-execs itself
    once per probe, so without one every child exits 127 and the
    rung below reads `unsupported` instead of qemu's `namespace`.
  version         podbox 0.1.0             rc=0
  rung, aarch64   unsupported   ⛔ QEMU'S ANSWER, NOT AN ARM MACHINE'S
  rung, host      namespace   the same host, measured natively
    probes: clone(NEWNS)=EINVAL mount=skip fsmount=skip move_mount=skip
      Seccomp: 0
                     got  unshare(CLONE_NEWNS)=skip clone(CLONE_NEWNS)=EINVAL mount(tmpfs,/mnt)=skip mount(tmpfs,/mnt) in clone(NEWNS)=skip
      mount(tmpfs,/mnt) in clone(NEWNS)
      mount(MS_SLAVE,/) in clone(NEWNS)
      fsmount(tmpfs)
          the child for "fsmount(tmpfs)" ended exit 127 and said nothing this parent can read; no verdict was established
  ⚠ Read those rows before quoting the rung. qemu-user does not
    implement fsmount, refuses clone(CLONE_NEWNS) with EINVAL and
    reports Seccomp 0 whatever the host is under. Every one of
    those is a statement about the EMULATOR.
  ⭐ What it does prove: the syscall numbers, the struct layouts
    and the trap are right for aarch64. A wrong statfs layout
    gives garbage block counts, and the writable rows are sane.

== 5. binfmt_misc with F, which is how a foreign image will run
  SKIP: binfmt_misc is not mounted here

== 7. podbox's OWN syscall trap, executed rather than only compiled
  binary          ELF 64-bit LSB executable, 64-bit PowerPC or cisco 7500, O
  bytes           2350984
  version         podbox 0.1.0             rc=0
  reads its own   ELF machine 0x15   (0x15 is PowerPC64)
  probe --json parses and carries a rung: rc=0
  ⚠ the rung under qemu is `unsupported` and that is CORRECT: no
    binfmt entry selects ELF machine 0x15 here, so every probe
    CHILD exits 127 and no verdict is established. What clause 7
    measures is the parent, which is where the trap runs.
